import { type SchemaTypeDefinition } from 'sanity'

import { ProductSchehma } from './product'

export const schema: { types: SchemaTypeDefinition[] } = {
  types: [ProductSchehma,], //add schehma here
}


// to do anything in sanity u need schema/modal first
