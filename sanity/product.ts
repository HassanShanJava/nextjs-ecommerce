export const ProductSchehma={
    name:"product", //namem we will use to fetchc data, its ur key for object
    type:"document", //defines a modal which has field
    title:"Product", //this is ur field name, usued only for sanity studio 
    // fields ->use array
    fields:[
        {
            name:"title",
            title:"Title",
            type:"string"
        }
    ]


}